# 🎰 Poker Tournament API

API для управления турнирами по покеру с полной системой аутентификации.

## 🚀 Быстрый старт


2. Запустите PostgreSQL:
```bash
docker-compose up db -d
```

3. Активируйте виртуальное окружение и запустите:
```bash
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 🐳 Продакшн (Docker)

1. Создайте `.env` файл с настройками для продакшна

2. Запустите весь стек:
```bash
docker-compose up --build -d
```

3. Проверьте логи:
```bash
docker-compose logs -f app
```

4. Остановить:
```bash
docker-compose down
```

## 📡 API Эндпоинты

**Базовый URL:** `http://localhost:8000/v1`

### Аутентификация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `POST /auth/refresh-token` - Обновить токен
- `GET /auth/me` - Информация о пользователе

### Пользователи
- `GET /user/me` - Мой профиль
- `PUT /user/me` - Обновить профиль

### Турниры
- `POST /tournaments/` - Создать турнир
- `GET /tournaments/my_tourney/` - Получить турниры
- `PUT /tournaments/{id}` - Обновить турнир
- `DELETE /tournaments/{id}` - Удалить турнир

## 📚 Документация

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
